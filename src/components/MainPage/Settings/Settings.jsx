import classNames from './Settings.module.css'

const Settings = () => {
    return (
        <div className={classNames.content}>
            <h2>Settings</h2>
        </div>
    );
}

export default Settings